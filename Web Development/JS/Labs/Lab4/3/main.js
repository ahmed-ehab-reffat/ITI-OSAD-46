var para = document.getElementById("PAR");

function ChangeFont(fontFamily) {
  para.style.fontFamily = fontFamily;
}

function ChangeAlign(alignment) {
  para.style.textAlign = alignment;
}

function ChangeHeight(hieght) {
  para.style.lineHeight = hieght;
}

function ChangeLSpace(letterSpacing) {
  para.style.letterSpacing = letterSpacing;
}

function ChangeIndent(indentation) {
  para.style.textIndent = indentation;
}

function ChangeTransform(transform) {
  para.style.textTransform = transform;
}

function ChangeDecorate(decorate) {
  para.style.textDecoration = decorate;
}

function ChangeBorder(borderStyle) {
  para.style.borderStyle = borderStyle;
}

function ChangeBorderColor(borderColor) {
  para.style.borderColor = borderColor;
}
