const id = require('./id');
const {newProduct, updateProduct} = require('./products');
const {newUser, updateUser} = require('./users');

module.exports = {
  id,
  newProduct,
  updateProduct,
  newUser,
  updateUser
};
